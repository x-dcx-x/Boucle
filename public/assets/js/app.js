
    // Boucle de 1 à 10
    for (let i = 1; i <= 10; i++) {
    // Vérifier si le nombre est pair ou impair
    let message = (i % 2 === 0) ? "Pair" : "Impair";

    // Afficher le message dans la console
    console.log("Le nombre " + i + " est " + message);
}
